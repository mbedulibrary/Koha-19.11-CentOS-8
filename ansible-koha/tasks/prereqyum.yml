---
  - name: install prereqs through yum
    yum:
     name: "{{ item }}"
    with_items:
     - php
     - mariadb
     - perl
     - cpanminus
     - ImageMagick
     - libxslt-devel 
      #- tcp_wrappers-devel - removed from CentOS 8 repos
     - rpm-cron 
     - memcached 
     - tcl 
     - libicu-devel 
     - setools 
     - gcc 
     - GraphicsMagick-perl 
     - libuuid-devel 
     - fribidi-devel 
     - bzip2-devel
